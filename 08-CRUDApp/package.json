{
  "name": "08-crudapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server server/db.json --port 3001 --snapshots server/snapshots/"
  },
  "devDependencies": {
    "json-server": "^0.17.3",
    "vite": "^4.2.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
